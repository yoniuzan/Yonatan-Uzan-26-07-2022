export namespace Enums {

    export enum WeatherTab {
        Home = 1,
        Favorite = 2
    }

    export enum Degrees {
        Fahrenheit  = 1,
        Celsius = 2
    }

    export enum Theme {
        Light  = 1,
        Dark = 2
    }
}